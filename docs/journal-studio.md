# Journal Studio：ブラウザーとローカルで記事を書く

2026-09-09。ローカルでのデザイン・操作確認を完了し、ユーザー承認を得てPRレビューと本番反映へ進める。

## 採用範囲

管理一覧・検索/タグ/種類/状態の絞り込み・並べ替え・一覧操作・削除/復元・公開設定・インポート改善（提案①〜⑥）と、メタデータ編集・原稿差し替え/履歴・ブラウザー執筆/自動保存（⑧〜⑩）。一括操作・予約公開・一括エクスポート（⑦⑪⑫）は含めない。

## 起動して確認する

`npm run journal:preview` を実行して `http://127.0.0.1:3003/journal/admin` を開く。ローカル専用D1と署名付きの確認用認証を使い、本番データには接続しない。既存ローカル記事は保持し、不足する列だけ追加する。変更後は停止して再起動すると再ビルドする。

- 「記事を書く」でIDを自動生成し、空の非公開下書きを作る。
- タイトル・要約・本文・タグ・種類・言語を編集。入力が止まって約1.5秒後に自動保存する。タグは入力欄を離れると確定する。
- 「公開設定へ」で保存済み下書きのプレビューを確認し、全体公開・限定公開・非公開を選ぶ。公開にはタイトル・要約・本文が必要。
- 公開後も編集できる。保存するだけでは読者が見る公開版は変わらない。「変更を反映する」で公開版を更新する。
- 変更履歴では保存済みの版を比較し、過去の版を下書きに戻せる。公開版はそのまま。変更箇所を含むブロックを色分けする。
- 競合や通信失敗では自動保存を停止して原稿を画面に保持する。再試行・Markdownダウンロード・保存版の読み直しを選べる。

一覧は削除用の44pxアイコンボタンと3点メニューを表示する。プレビュー・編集・Markdown保存・公開設定・復元はメニュー内。削除とメニューのボタンはホバーとキーボードフォーカスで操作名を表示する。メニューはEscapeまたはフォーカスが外れると閉じる。

## ローカルで作成する

```sh
npm run journal:new -- --title "記事のタイトル"
npm run journal:new -- --title "記事のタイトル" --dir /path/to/private-journal
npm run journal:new -- --template /path/to/custom-template.md --dir /path/to/private-journal
```

既定のテンプレートは `templates/journal.md`。IDと日本時間の日付を自動生成して、`<id>.md` を作る。IDはUUIDを用い、クラウドでも同じ形式で自動生成する。既存ファイルは上書きしない。既定の保存先はGit除外済みの `.journal-private/`。`JOURNAL_DIRECTORY` または `--dir` でリポジトリ外の原稿置き場を指定できる。ファイル権限は本人のみの読み書き。

記事IDはタイトルやファイル名を変えても維持する。ブラウザーで作った記事も「Markdownを保存」で取り出せる。ローカルで修正した後は、インポートまたは記事の「Markdownで差し替え」で同じ記事の下書きへ取り込む。同じIDがあれば保存版と比較し、明示操作で取り込む。IDのない新規ファイルはインポートの確認時に自動生成する。

自動同期・監視・CLIからの自動公開は行わない。既存の `journal:send` は認証付きの新規取り込み・同一原稿再送の互換経路として維持し、競合する原稿の強制上書きは行わない。

## 削除

- 公開停止：原稿を残して非公開にする。
- ゴミ箱：通常一覧から除外し、公開URLと共有リンクを停止する。
- 復元：必ず非公開。過去の共有リンクは復活しない。
- 完全削除：ゴミ箱内の対象記事を確認し、保存原稿・変更履歴・公開/共有設定を削除する。ローカルファイルや外部参照画像、別途取得済みのバックアップは消さない。

自動でゴミ箱を空にしない。操作はすべて管理者認証・同一Origin・version競合検査を通す。

## 保存方式と公開境界

既存IDとURLは変更しない。保存原稿はrevisionとして追記し、下書きの参照だけ更新する。公開中の参照は公開操作まで保持する。メタデータも下書きと一緒に反映する。以前のタグ専用APIの既存動作は互換性のため維持する。

プレビューは認証済みAPIで公開記事と同じMarkdown処理・サニタイズ・数式処理を使う。外部画像を読み込まず、リンクは無効化する。Mermaidはクライアント側で描画する。原稿はビルド入力や公開バンドルへ含めない。

履歴一覧は50件ずつ取得。管理一覧は検索条件をURLに保持し、50件ずつ取得する。保存日時順は同時刻にIDで順序を固定する。APIは受信サイズを制限し、SQLはパラメーター化する。

## リリース前

ユーザーによるローカル確認は完了。PRレビューと必須チェック通過後にマージする。本番には `0003_journal_editing.sql` と `0004_journal_saved_at.sql` の追加適用が必要。記事の削除・初期化はmigrationやデプロイに含めない。認証方式・権限は変更しない。

## ローカル検証結果

- Lint・TypeScript・本番ビルド成功。非公開原稿が生成バンドルに含まれないことをpostbuildで確認。
- Journalの39テスト成功：既存認証・競合・バックアップ互換性、新規ID、空の下書き、保存/公開の分離、履歴、ゴミ箱/復元/完全削除、CLIのファイル生成。
- 実際のbuilt Workerと隔離D1を使うブラウザー回帰が成功：新規作成、Markdownインポート、入力・自動保存、古い別タブの保存拒否と入力保持、履歴復元、限定公開/停止、検索、削除/復元/完全削除。
- タグ編集/削除と公開反映、既存の公開日・並び替え・ページ送りの回帰が成功。
- 320/390/1440pxの一覧・エディター・インポート画面には横はみ出しなし。実際のローカル画面で日本語入力、ライト/ダーク、プレビュー表示を確認。ブラウザーの警告・エラーなし。
- 既存コンテンツ12テスト、29ルートSSR、8ルート×日英検証も成功。

実端末のSafari・本番Access認証・本番DB migrationは未実施。PRレビューと本番確認はリリース時に記録する。

2026-09-09 UI確認後の調整：削除だけ一覧に残し、他の操作を3点メニューに集約。Notes共通見出しを「書いたこと、／のこしたもの。」、Projectsを「つくったもの、／ほしかったもの。」へ変更。

PRレビューで旧形式のタグ引き継ぎと公開/共有リンク更新中の入力保持を修正。管理APIは更新した状態を応答で返し、更新直後の再読み込みに依存しない。`node scripts/verify-journal-editor-regressions.mjs` で、編集せず初回公開した場合のタグ保持、再読み込みGETが失敗する条件でも公開状態を更新できること、公開/共有リンク再発行の送信・応答を保留した間の入力が下書きだけに保存されることを確認。修正前の不具合を再現し、修正後の回帰と既存Journalブラウザー検証が成功。

履歴読み込み中に入力した場合は、未保存のまま復元しない。復元の応答待ちにダイアログを閉じて入力しても、その原稿を保持する。保存版の手動再読み込みも、確認後に入力が変わった場合は中止する。追加の回帰では自動保存の時計を停止し、履歴GET・復元POST・保存版GETをそれぞれ遅延させ、入力が消えないことを確認した。

インポート前の保存待ちにキャンセルボタンまたはEscapeで閉じた場合は、保存後もキャンセルを維持する。確定処理は保存後に同じインポートが選択中か照合する。両方のキャンセル操作を、コミット済み保存応答を遅延させた状態で確認し、既存の入力だけが保存されることを検証した。

## 執筆区分（AI生成について）

ブラウザーの記事設定とMarkdown取り込み画面で、`人間が執筆` / `AI生成` / `未設定` を選択する。Markdownのfront matterには `authorship: human | ai | unknown` として保存される。フィールドのない既存原稿は `unknown` として扱い、本文や `sourceType` から推測しない。

- `human`: 記事一覧カード・記事本文のヘッダー・管理プレビューに公式Not By AIライターバッジを表示。
- `ai`: 同じ場所に「AI生成」ラベルを表示。
- `unknown`: 読者向け表示なし。取り込み後でも記事設定から変更可能。

区分は原稿リビジョンに含まれるため、下書きの自動保存だけでは公開中のバッジは変わらない。公開設定で反映した版の区分を表示する。ダウンロード、再取り込み、変更履歴からの復元でも区分を保持する。新規インポートと編集画面のMarkdown差し替えではファイルの区分を初期選択し、取り込み前に変更できる。

ローカル作成例: `npm run journal:new -- --title "記事のタイトル" --authorship ai`。省略時はテンプレートの値、なければ `unknown`。既存の `journal:send` も同じfront matterを受け付ける。

### Not By AIの素材と条件

公式SVGを `public/not-by-ai/written-by-human.svg` に保存し、外部ホットリンクはしない。元画像の131 × 42 px、周囲14 pxの余白を保ち、色や文言を改変しない。

- 取得元: https://notbyai.fyi/img/jp/written-by-human-not-by-ai-white-jp.svg
- 利用ガイドライン: https://notbyai.fyi/help/guidelines
- ライセンス: https://notbyai.fyi/license （2026-09-10確認）

バッジは著者の自己申告であり検出・認証結果ではない。90%以上を人間が制作し、核となる意味や創作の方向をAIに委ねていないことが条件。商用利用には有料の許諾が必要なため、公開前にサイトでの利用条件を所有者が確認する。個人ポートフォリオの商用・非商用の境界は規約に明記されていないため、この実装から利用区分を断定しない。

検証: `node scripts/verify-journal-authorship.mjs`（ビルド後）。隔離したローカルD1で作成・保存・取り込み・差し替え・公開版の分離と、PC / スマートフォンの表示を確認する。デザイン用サンプルは実記事の執筆区分を示すものではない。

表示言語が英語の場合は公式英語版 `written-by-human-en.svg`（取得元: https://notbyai.fyi/img/written-by-human-not-by-ai-white.svg）へ切り替える。記事の本文言語ではなくサイトの表示言語に連動し、バッジの代替テキスト・リンク先・AI生成ラベルも切り替える。
